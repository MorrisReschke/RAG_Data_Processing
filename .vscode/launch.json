{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run main as module",
            "type": "debugpy",
            "request": "launch",
            "module": "main",
            "cwd": "${workspaceFolder}"
        },
    ]
}